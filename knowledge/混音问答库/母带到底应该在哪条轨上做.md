# 母带到底应该在哪条轨上做

**症状：** 看到软件里有叫 `Master` 的轨道，就以为母带必须只在那上面处理。

**原因：** 软件命名和母带这个概念容易混在一起。

**解决：** 只要处理对象是“完整立体声成品”，是在默认 master 轨、总 Bus，还是单独导出后另开工程来做，都可以。

**常见误区：** 以为不在软件默认 `Master` 轨上做，就不算母带处理。

**关键词：** #母带, #工作流, #StudioOne
