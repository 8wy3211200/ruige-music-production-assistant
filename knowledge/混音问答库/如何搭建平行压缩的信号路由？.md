# 如何搭建平行压缩的信号路由？

**症状：** 知道平行压缩的好处，但不知道如何在DAW中具体实现。

**原因：** DAW没有现成的平行压缩路由，需要手动搭建信号链。

**解决：** 创建一条干声轨道（推子0dB不动）和多条压缩辅助轨道。从干声轨道发送至各压缩辅助轨道（每条轨道串一个不同型号压缩器），各辅助轨道推子根据需求降低（如-6dB、-10dB），最后各轨道汇入同一总线。关键是干声和各压缩轨道之间互不影响。

**常见误区：** 把压缩器直接串联在轨道上，或在一条轨道上挂多个压缩器调干湿比——这不是真正的平行压缩。

**关键词：** #压缩, #动态

---
