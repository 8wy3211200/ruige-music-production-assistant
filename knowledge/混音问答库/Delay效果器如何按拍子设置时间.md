# Delay效果器如何按拍子设置时间

## Q1: Delay效果器如何按拍子设置时间

**症状：** 不清楚Delay的时间参数应该怎么调，是按毫秒还是按拍子

**原因：** 不了解音符时值（四分、二分、八分音符）与节拍的关系

**解决：**
- 四分音符 = 每拍重复一次（最常用）
- 二分音符 = 每两拍重复一次
- 八分音符 = 每半拍重复一次
- 具体操作：打开Delay插件，找到Note/Time参数，选择对应音符时值

**常见误区：** 直接调毫秒数值，调到奇怪的位置导致重复声与音乐节拍不和谐

**关键词：** #插件, #混响
