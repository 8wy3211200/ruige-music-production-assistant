# 多段压缩器如何用于人声处理？

**症状：** 用户在效果链中看到MVR（可能是多段压缩器），不熟悉这个工具。

**原因：** 多段压缩器是较高级的处理工具，将音频分成多个频段分别压缩，用户接触较少。

**解决：** 多段压缩器的优势在于可以针对不同频段设置不同的压缩参数。例如：低频段设置较大压缩比控制低音动态，高频段设置较小压缩比保持瞬态。具体参数需根据素材特点调整，核心原理是"向上向下同时进行的压缩"——既压峰值也抬低谷值，让动态更加均衡。

**常见误区：** 不了解多段压缩的频段划分逻辑，随便调整参数导致声音变得奇怪。

**关键词：** #压缩 #人声 #动态
