# 侧链压缩里的 EQ 到底在干什么

**症状：** 侧链做完后，贝斯避让得太多、太久，整个低频变得空。

**原因：** 压缩器默认“听到”的是完整的 Kick 音色，包含大量低频体积感，触发会过重。

**解决：** 用侧链里的 EQ 只保留 Kick 的瞬态和更短促的触发信息，把臃肿的低频削掉。这样压缩器“听见”的是更短的一下，贝斯就只会短暂闪避。

**常见误区：** 以为侧链 EQ 会改变最终听到的 Kick 或 Bass 音色。

**关键词：** #混音, #动态, #效果器
