# 总线上的默认 Limiter 该不该留

**症状：** 监看表头和响度时，总线末端还挂着宿主默认的 limiter。

**原因：** 没意识到它会改变监听判断，还可能让参考轨一起被处理。

**解决：** 混音阶段应尽量保持总线干净，至少不要让默认 limiter 干扰判断；如果真要做响度处理，也应该在正确位置使用，并与参考监听逻辑分开。

**常见误区：** 看到 DAW 默认带着就一直不删，以为那是“必须存在”的配置。

**关键词：** #混音, #母带, #工作流
