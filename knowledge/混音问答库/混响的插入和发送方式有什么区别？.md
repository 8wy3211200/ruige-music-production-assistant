# 混响的插入和发送方式有什么区别？

**核心要点：** 插入式混响直接处理干声，发送式混响通过效果通道添加，不影响原始干声调节。

**详细解答：** 插入式将混响插件直接放在轨道效果链中，会改变原始干声信号。发送式在调音台创建效果通道放置混响，从轨道发送信号到效果通道，通过发送电平控制混响量。人声混音常用发送式，避免影响已调好的干声。发送式混响需将干湿比调至100%，用发送量控制效果强度。插入式适合需要直接改变音色的情况。

**应用场景：** 混响使用方式选择、人声效果处理、效果链设计

**关键词：** #混响方式 #插入式 #发送式 #效果处理
